#@title Test the output

projector_2=np.random.random((64,2))
mid_projector=Model(inputs=inputs,outputs=ann_model.get_layer('mid').output)
final_proj=mid_projector.predict(proj_1_test[0])
project=np.matmul(proj_1_test[0],projector_2)

import seaborn as sns
sns.scatterplot(project[:,0],project[:,1],labels_test,palette="Dark2")

from sklearn.svm import SVC
from sklearn.metrics import accuracy_score as acc
Xtrain,ytrain=project[0:100],labels_test[0:100]
Xtest,ytest=project[101:],labels_test[101:]
classifier=SVC(kernel='rbf').fit(Xtrain,ytrain)
acc(ytest,classifier.predict(Xtest))
